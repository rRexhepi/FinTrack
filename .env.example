# Copy to `.env` and fill in real values. `.env` is gitignored.

# Django
# Generate a fresh key with: python get_secret.py
SECRET_KEY=replace-me
DEBUG=True

# Postgres
DB_NAME=fintrack_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
